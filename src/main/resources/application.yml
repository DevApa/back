server:
  port: 89

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db_secoed
    username: dba
    password: dba2021
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      jdbc:
        lob:
          non_contextual_creational: true

